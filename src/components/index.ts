/**
 * exports two components from different folders
 * @description makes exporting easier rather than exporting them one by one
 */
export { default as Navbar } from './navbar/Navbar';
export { default as Shop } from './shop/Shop';